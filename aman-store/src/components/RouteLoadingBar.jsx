const RouteLoadingBar = () => (
  <div
    className="fixed inset-x-0 top-0 z-[70] h-0.5 bg-gradient-to-r from-transparent via-green-600 to-transparent opacity-90 animate-pulse"
    aria-hidden
  />
);

export default RouteLoadingBar;
